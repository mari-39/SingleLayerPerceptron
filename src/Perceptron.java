public class Perceptron {
    double[][] inputs;
    double[] weights;

    public Perceptron(double[][] inputs, double[] weights) {
        this.inputs = inputs;
        this.weights = weights;
    }
}
